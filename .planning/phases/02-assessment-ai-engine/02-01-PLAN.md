---
phase: 02-assessment-ai-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/demo-data.ts
  - src/lib/assessment/types.ts
  - src/lib/assessment/questions.ts
  - src/lib/assessment/scoring.ts
  - src/lib/assessment/schemas.ts
  - src/lib/assessment/questions.test.ts
  - src/lib/assessment/scoring.test.ts
  - src/lib/assessment/schemas.test.ts
autonomous: true
requirements:
  - SCAN-01
  - SCAN-03
  - SCAN-04
  - SCORE-01
  - SCORE-02
  - SCORE-06

must_haves:
  truths:
    - "Assessment has 5 business-centric sections: Operations, Sales, Finance, Team, Risks"
    - "Each section has 3-4 plain-language business questions (15-20 total)"
    - "Business Health Score uses levels Critical/Struggling/Stable/Efficient/Optimized"
    - "Dimension keys are operations/sales/finance/team/risks everywhere"
    - "All tests pass with new dimension names and content"
  artifacts:
    - path: "src/lib/assessment/types.ts"
      provides: "Dimension union type with new business keys"
      contains: "operations.*sales.*finance.*team.*risks"
    - path: "src/lib/assessment/questions.ts"
      provides: "5 business-centric sections with 15-20 questions"
      min_lines: 150
    - path: "src/lib/assessment/scoring.ts"
      provides: "Business health levels and business-focused keywords"
      contains: "Critical.*Struggling.*Stable.*Efficient.*Optimized"
    - path: "src/lib/assessment/schemas.ts"
      provides: "Zod schemas with new dimension keys"
      contains: "operations.*sales.*finance.*team.*risks"
    - path: "src/lib/demo-data.ts"
      provides: "MaturityScore interface + demo data with new dimension keys"
      contains: "operations.*sales.*finance.*team.*risks"
  key_links:
    - from: "src/lib/assessment/types.ts"
      to: "src/lib/demo-data.ts"
      via: "re-export of MaturityScore"
      pattern: "export type.*MaturityScore.*from.*demo-data"
    - from: "src/lib/assessment/schemas.ts"
      to: "src/lib/demo-data.ts"
      via: "z.infer type must match MaturityScore.dimensions"
      pattern: "operations.*sales.*finance.*team.*risks"
    - from: "src/lib/assessment/questions.ts"
      to: "src/lib/assessment/types.ts"
      via: "dimension field values match union type"
      pattern: "dimension:.*operations|sales|finance|team|risks"
---

<objective>
Atomic data layer rewrite: rename all 5 dimension keys from AI-maturity (strategy/adoption/riskManagement/roiTracking/governance) to business-health (operations/sales/finance/team/risks), replace all questions with business-focused content, update scoring levels, and update all tests.

Purpose: This is the foundational content change that all other files depend on. Dimension keys appear in 8+ files and must change atomically to avoid runtime breaks.
Output: All assessment data definitions, questions, scoring, schemas, demo-data, and tests updated for business-first approach.
</objective>

<execution_context>
@/Users/alextokarenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alextokarenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assessment-ai-engine/02-CONTEXT.md
@.planning/phases/02-assessment-ai-engine/02-RESEARCH.md

<interfaces>
<!-- Current types that MUST change. Executor should use these as reference for what to replace. -->

From src/lib/demo-data.ts (canonical type definition):
```typescript
export interface MaturityScore {
  overall: number;
  dimensions: {
    strategy: number;      // -> operations
    adoption: number;      // -> sales
    riskManagement: number; // -> finance
    roiTracking: number;   // -> team
    governance: number;    // -> risks
  };
}
```

From src/lib/assessment/types.ts:
```typescript
export interface AssessmentSection {
  id: string;
  name: { en: string; ru: string };
  dimension: "strategy" | "adoption" | "riskManagement" | "roiTracking" | "governance";
  // -> "operations" | "sales" | "finance" | "team" | "risks"
  questions: Question[];
}
```

From src/lib/assessment/scoring.ts:
```typescript
export function getMaturityLevel(score: number): { level: string; range: string }
// Rename to getHealthLevel, levels: Critical/Struggling/Stable/Efficient/Optimized
```

From src/lib/assessment/schemas.ts:
```typescript
// scoreAndRoadmapSchema.maturityScore.dimensions has keys:
// strategy, adoption, riskManagement, roiTracking, governance
// -> operations, sales, finance, team, risks
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic dimension rename across types, schemas, scoring, and demo-data</name>
  <files>src/lib/demo-data.ts, src/lib/assessment/types.ts, src/lib/assessment/scoring.ts, src/lib/assessment/schemas.ts</files>
  <action>
Rename dimension keys across 4 files in this exact order (to maintain type consistency):

1. **src/lib/demo-data.ts** (canonical type definition -- change FIRST):
   - `MaturityScore.dimensions`: rename keys `strategy->operations`, `adoption->sales`, `riskManagement->finance`, `roiTracking->team`, `governance->risks`
   - Update ALL 3 demo company `dimensions` objects with same key rename (keep score values)
   - Keep all other interfaces (RoadmapItem, RiskItem, etc.) unchanged

2. **src/lib/assessment/types.ts**:
   - `AssessmentSection.dimension` union: change to `"operations" | "sales" | "finance" | "team" | "risks"`
   - Keep all other types (Question, FollowUpQuestion, VALID_TRANSITIONS, AssessmentStatus) unchanged
   - Re-exports from demo-data.ts remain unchanged

3. **src/lib/assessment/scoring.ts**:
   - Rename `getMaturityLevel` to `getHealthLevel`
   - Update level names: Beginner->Critical, Developing->Struggling, Intermediate->Stable, Advanced->Efficient, Leader->Optimized
   - Replace keyword list with business-focused terms: "manual", "bottleneck", "process", "automate", "automation", "customer", "revenue", "cost", "expense", "invoice", "employee", "turnover", "hiring", "training", "compliance", "risk", "deadline", "inventory", "hours", "time", "waste", "inefficient", "complaint", "spreadsheet", "report", "pipeline", "conversion"
   - Keep `calculateDimensionScores`, `calculateOverallScore`, `calculateProgress` logic unchanged (they are dimension-key-agnostic)
   - Keep `scoreOpenText` internal function structure but update its keyword list

4. **src/lib/assessment/schemas.ts**:
   - `scoreAndRoadmapSchema.maturityScore.dimensions`: rename 5 keys to operations/sales/finance/team/risks
   - Update `.describe()` strings: "Operations and Processes score 0-100", "Sales and Customers score 0-100", "Finance and Resources score 0-100", "Team and HR score 0-100", "Risks and Compliance score 0-100"
   - Update the top-level `.describe()` on `maturityScore` from AI maturity language to business health language
   - Keep `followUpSchema` and `riskAndRoiSchema` structurally unchanged (they have no dimension keys)

CRITICAL: After changes, run TypeScript compiler to verify NO type errors across the project. The dimension union in types.ts constrains questions.ts -- compiler will catch any mismatch.

Do NOT touch: Prisma schema, actions.ts, any UI components, prompts.ts, i18n files.
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 4 files have new dimension keys. TypeScript compiles with zero errors. No file references old dimension names (strategy/adoption/riskManagement/roiTracking/governance) in code (string literals in prompts.ts and i18n are handled by Plan 02).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite questions with business-focused content</name>
  <files>src/lib/assessment/questions.ts</files>
  <action>
Replace ALL 5 sections in ASSESSMENT_SECTIONS with business-centric content per CONTEXT.md locked decisions. Keep the data structure (AssessmentSection[]) and helper functions (getQuestionsForSection, getTotalQuestionCount) unchanged.

**Section 1: Operations and Processes** (dimension: "operations", id: "operations")
3-4 questions about: manual/repetitive work that consumes time, bottlenecks in workflows, things that break regularly, desire to automate. Mix of open-text and single-choice. Questions must use plain business language -- "What tasks eat up the most time?" not "Rate your process maturity."

**Section 2: Sales and Customers** (dimension: "sales", id: "sales")
3-4 questions about: how they find customers, where they lose them, conversion pipeline state, customer complaints/feedback handling. Lead with pain: "Where do you lose customers?"

**Section 3: Finance and Resources** (dimension: "finance", id: "finance")
3-4 questions about: fastest-growing costs, manual reporting/invoicing, cash flow pain points, resource allocation visibility. Plain language.

**Section 4: Team and HR** (dimension: "team", id: "team-hr")
3-4 questions about: hiring bottlenecks, employee turnover causes, training gaps, communication overhead. Relatable: "What makes good employees leave?"

**Section 5: Risks and Compliance** (dimension: "risks", id: "risks-compliance")
3-4 questions about: what keeps owner up at night, regulatory burden, data security concerns, single points of failure. Conversational: "What would happen to your business if [key person] left tomorrow?"

Requirements for ALL questions:
- Bilingual text (en + ru) -- Russian should be natural, not a literal translation
- Each question has unique id (prefix with section abbreviation: ops_, sal_, fin_, thr_, rsk_)
- Weight 1-3 (higher = more important for that dimension)
- Mix of open-text and single-choice (at least 1 single-choice per section for quick signals)
- Single-choice options should have 3-4 options with bilingual text
- Total across all sections: 15-20 questions
- Tone: like talking to a smart business consultant, not filling out a tech survey
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>ASSESSMENT_SECTIONS has 5 business-centric sections with 15-20 total questions. Every question has bilingual text, unique id, appropriate weight and type. Questions feel conversational and focus on business pain.</done>
</task>

<task type="auto">
  <name>Task 3: Update all test files for new content</name>
  <files>src/lib/assessment/questions.test.ts, src/lib/assessment/scoring.test.ts, src/lib/assessment/schemas.test.ts</files>
  <action>
Update all 3 test files to match the new business-first content:

1. **questions.test.ts**:
   - Update "covers all 5 dimensions" test: expect "operations", "sales", "finance", "team", "risks" instead of old dimension names
   - All other tests (5 sections, 3-4 questions each, bilingual, unique ids, weights, 15-20 total) should pass without changes since they check structural invariants, not specific content

2. **scoring.test.ts**:
   - `calculateDimensionScores` "returns all 5 dimension keys": expect "operations", "sales", "finance", "team", "risks"
   - `calculateOverallScore` tests: update dimension key names in test data objects (strategy->operations, adoption->sales, etc.)
   - `getMaturityLevel` tests: rename to `getHealthLevel`, update level names in test cases (Beginner->Critical, Developing->Struggling, Intermediate->Stable, Advanced->Efficient, Leader->Leader->Optimized)
   - `calculateDimensionScores` "single-choice answers apply weights correctly": update `scores.strategy` -> `scores.operations`
   - Import `getHealthLevel` instead of `getMaturityLevel`

3. **schemas.test.ts**:
   - `scoreAndRoadmapSchema` validData: rename dimension keys in `maturityScore.dimensions` object
   - "output matches MaturityScore interface shape" test: check for new dimension keys (operations, sales, finance, team, risks)
   - "rejects missing dimensions" test: update partial object to use new key
   - "type compatibility" tests: update all `dimensions` objects to use new keys
   - Keep riskAndRoiSchema tests unchanged (no dimension keys in those schemas)

After updating all tests, run the full test suite to verify everything passes.
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>All tests pass. Test assertions reference new dimension keys (operations/sales/finance/team/risks) and new health level names (Critical/Struggling/Stable/Efficient/Optimized). Zero test failures.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors
2. `npx vitest run --reporter=verbose` -- all tests pass
3. Grep confirmation: `grep -rn "strategy\|adoption\|riskManagement\|roiTracking\|governance" src/lib/assessment/ src/lib/demo-data.ts` should return ZERO hits (all old dimension names removed from data layer)
4. Grep confirmation: `grep -rn "Beginner\|Developing\|Intermediate\|Advanced\|Leader" src/lib/assessment/` should return ZERO hits (all old level names removed)
</verification>

<success_criteria>
- 5 business-centric sections exist in questions.ts with 15-20 total questions
- All dimension keys are operations/sales/finance/team/risks across types, questions, scoring, schemas, demo-data
- Health levels are Critical/Struggling/Stable/Efficient/Optimized in scoring.ts
- All unit tests pass with new content
- TypeScript compiles without errors
- No references to old dimension names (strategy/adoption/riskManagement/roiTracking/governance) remain in data layer files
</success_criteria>

<output>
After completion, create `.planning/phases/02-assessment-ai-engine/02-01-SUMMARY.md`
</output>
