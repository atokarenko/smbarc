---
phase: 04-module-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/modules/catalog-data.ts
  - src/lib/modules/recommendation.ts
  - src/lib/modules/catalog-data.test.ts
  - src/lib/modules/recommendation.test.ts
  - src/app/[locale]/(app)/modules/page.tsx
  - src/app/[locale]/(app)/modules/modules-content.tsx
  - src/messages/en.json
  - src/messages/ru.json
autonomous: true
requirements:
  - MOD-01
  - MOD-02
  - MOD-03

must_haves:
  truths:
    - "User can browse a catalog of AI modules organized by 4 categories (HR, Finance, Legal, Decision Making)"
    - "Each module card shows name, description, expected impact, and relevance badge based on assessment data"
    - "Modules are sorted by relevance -- struggling business areas surface higher-relevance modules first"
    - "Catalog works without assessment data -- shows modules without personalized scores, with CTA to complete assessment"
    - "All text renders in both English and Russian"
  artifacts:
    - path: "src/lib/modules/catalog-data.ts"
      provides: "Static module definitions with i18n keys, 10-12 modules across 4 categories"
      exports: ["CatalogModule", "CATALOG_MODULES"]
    - path: "src/lib/modules/recommendation.ts"
      provides: "Dimension-based relevance scoring"
      exports: ["computeRelevance", "getRecommendedModules"]
    - path: "src/lib/modules/recommendation.test.ts"
      provides: "Unit tests for recommendation scoring"
      contains: "computeRelevance"
    - path: "src/lib/modules/catalog-data.test.ts"
      provides: "Data integrity tests for catalog"
      contains: "categories"
    - path: "src/app/[locale]/(app)/modules/page.tsx"
      provides: "Server component loading assessment data"
      contains: "loadAssessmentData"
    - path: "src/app/[locale]/(app)/modules/modules-content.tsx"
      provides: "Client component rendering module catalog grid"
      contains: "ModulesContent"
    - path: "src/messages/en.json"
      provides: "English translations for modules section"
      contains: "modules"
    - path: "src/messages/ru.json"
      provides: "Russian translations for modules section"
      contains: "modules"
  key_links:
    - from: "src/app/[locale]/(app)/modules/page.tsx"
      to: "src/lib/demo-data.ts"
      via: "loadAssessmentData reuse from dashboard pattern"
      pattern: "loadAssessmentData"
    - from: "src/app/[locale]/(app)/modules/modules-content.tsx"
      to: "src/lib/modules/recommendation.ts"
      via: "computeRelevance called per module with dimension scores"
      pattern: "computeRelevance"
    - from: "src/lib/modules/catalog-data.ts"
      to: "src/messages/en.json"
      via: "module IDs used as i18n key paths"
      pattern: "modules\\.items\\."
---

<objective>
Build the AI module catalog page -- the final POC feature. Users browse 10-12 business automation solutions organized by category (HR, Finance, Legal, Decision Making), with personalized relevance scoring based on their assessment results.

Purpose: Complete the POC by showing users actionable next steps -- which AI automations would benefit their business most, based on identified pain points.
Output: Working /modules page with card grid, relevance badges, category filtering, bilingual support, and graceful no-assessment fallback.
</objective>

<execution_context>
@/Users/alextokarenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alextokarenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-module-catalog/04-RESEARCH.md

@src/lib/demo-data.ts
@src/app/[locale]/(app)/dashboard/page.tsx
@src/messages/en.json
@src/messages/ru.json

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From src/lib/demo-data.ts:
```typescript
export interface MaturityScore {
  overall: number;
  dimensions: {
    operations: number;
    sales: number;
    finance: number;
    team: number;
    risks: number;
  };
}

export interface AssessmentResults {
  maturityScore: MaturityScore;
  automationRoadmap: RoadmapItem[];
  riskMap: RiskItem[];
  roiForecast: ROIForecast;
}
```

From src/app/[locale]/(app)/dashboard/page.tsx:
```typescript
// loadAssessmentData pattern -- duplicate this for modules page (not shared util, matches project convention)
async function loadAssessmentData(userId: string): Promise<{
  results: AssessmentResults | null;
  companyName: string | null;
}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module data layer -- catalog definitions, recommendation logic, and tests</name>
  <files>src/lib/modules/catalog-data.ts, src/lib/modules/recommendation.ts, src/lib/modules/catalog-data.test.ts, src/lib/modules/recommendation.test.ts</files>
  <behavior>
    - catalog-data.test.ts: CATALOG_MODULES contains modules in all 4 categories (hr, finance, legal, decision-making)
    - catalog-data.test.ts: Every module has id, category, icon, relevantDimensions (non-empty), impactLevel
    - catalog-data.test.ts: All module IDs are unique
    - catalog-data.test.ts: relevantDimensions only reference valid dimension keys (operations, sales, finance, team, risks)
    - recommendation.test.ts: computeRelevance returns higher score for lower dimension values (struggling areas = more relevant)
    - recommendation.test.ts: computeRelevance returns 0-100 range
    - recommendation.test.ts: getRecommendedModules returns all modules sorted by relevance (highest first)
    - recommendation.test.ts: getRecommendedModules with null dimensions returns modules unsorted (no crash)
  </behavior>
  <action>
    Create src/lib/modules/ directory with 4 files:

    **catalog-data.ts:**
    - Export `CatalogModule` interface: { id: string, category: "hr" | "finance" | "legal" | "decision-making", icon: string (lucide icon name), relevantDimensions: string[], impactLevel: "high" | "medium" | "low" }
    - Export `CATALOG_MODULES: CatalogModule[]` with 11 modules using business-action names (not "AI Module" names):
      - HR (3): "hiring-pipeline" (UserSearch), "employee-onboarding" (UserPlus), "team-performance" (BarChart3) -- dimensions: team, operations
      - Finance (3): "invoice-processing" (Receipt), "cash-flow-forecasting" (TrendingUp), "expense-automation" (CreditCard) -- dimensions: finance, operations
      - Legal (2): "contract-review" (FileSearch), "compliance-monitoring" (Shield) -- dimensions: risks, finance
      - Decision Making (3): "business-intelligence" (LayoutDashboard), "risk-assessment" (AlertTriangle), "churn-prediction" (UserMinus) -- dimensions: operations, sales
    - Export `MODULE_CATEGORIES` array of category IDs for iteration

    **recommendation.ts:**
    - Import CatalogModule and MaturityScore dimensions type
    - `computeRelevance(module: CatalogModule, dimensions: MaturityScore["dimensions"]): number` -- average the dimension scores for module's relevantDimensions, invert (100 - avg), clamp to 0-100. Lower business health = higher module relevance.
    - `getRecommendedModules(modules: CatalogModule[], dimensions: MaturityScore["dimensions"] | null): (CatalogModule & { relevance: number })[]` -- if dimensions null, return modules with relevance 0. Otherwise compute relevance per module, sort descending.

    **Tests (TDD -- write first, then implement):**
    - catalog-data.test.ts: Test data integrity (all categories present, valid fields, unique IDs, valid dimension refs)
    - recommendation.test.ts: Test scoring logic (inversion, range, sorting, null safety)
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx vitest run src/lib/modules/ --reporter=verbose</automated>
  </verify>
  <done>All catalog data tests pass (4 categories, 11 modules, valid fields). All recommendation tests pass (correct inversion scoring, sorting, null safety). Zero new dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Module catalog UI -- page, cards, i18n, and recommendation display</name>
  <files>src/app/[locale]/(app)/modules/page.tsx, src/app/[locale]/(app)/modules/modules-content.tsx, src/messages/en.json, src/messages/ru.json</files>
  <action>
    **Update src/messages/en.json** -- add "modules" section:
    - modules.title: "AI Automation Modules"
    - modules.subtitle: "Business solutions recommended based on your assessment"
    - modules.noAssessment: "Complete your assessment for personalized recommendations"
    - modules.noAssessmentCta: "Start Assessment"
    - modules.recommended: "Recommended"
    - modules.relevance: "Relevance: {score}%"
    - modules.impact: "Expected Impact"
    - modules.impactLevels.high/medium/low
    - modules.comingSoon: "Coming Soon"
    - modules.categories.hr/finance/legal/decision-making: display names
    - modules.allCategories: "All"
    - modules.items.[each module id].name: Business-action name (e.g., "Automate Hiring Pipeline")
    - modules.items.[each module id].description: 1-2 sentence business description
    - modules.items.[each module id].impact: Expected business impact statement

    **Update src/messages/ru.json** -- same structure, Russian translations. Module names should be actionable: "Автоматизация найма", "Обработка счетов", etc.

    **Rewrite src/app/[locale]/(app)/modules/page.tsx** (server component):
    - Follow exact pattern from dashboard/page.tsx: get session, loadAssessmentData, extract role
    - Duplicate loadAssessmentData locally (project convention: no shared util, per decision [Phase 03-02])
    - Pass assessmentResults to ModulesContent

    **Create src/app/[locale]/(app)/modules/modules-content.tsx** (client component):
    - "use client" with useTranslations("modules")
    - Import CATALOG_MODULES, MODULE_CATEGORIES, getRecommendedModules
    - State: selectedCategory (null = all, or category string) for tab filtering
    - Layout: Page title + subtitle, category tabs (All + 4 categories using shadcn Tabs or simple button group), card grid
    - Card grid: responsive (1 col mobile, 2 cols md, 3 cols lg) using Tailwind grid
    - Each card (shadcn Card): lucide icon + category badge, module name (from i18n), description (from i18n), impact level badge (high=green, medium=amber, low=gray), relevance badge (if assessment data exists: "Recommended" for >70% relevance in primary variant, or "Relevance: X%" in secondary), "Coming Soon" muted label at bottom
    - No-assessment state: Show all modules without relevance badges, add banner at top with CTA linking to /scan
    - Dynamic icon rendering: create small helper that maps icon string to lucide component (import the ~11 specific icons used, switch/map by name)
    - Sort modules by relevance when assessment data available, alphabetically by id when not
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Modules page renders at /en/modules and /ru/modules. Shows 11 module cards in grid layout. Category tabs filter correctly. With assessment data: modules sorted by relevance with badges. Without assessment data: modules shown without relevance, CTA banner visible. All text bilingual. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/modules/` -- all data + recommendation tests pass
2. `npx next build` -- no build errors
3. Navigate to /en/modules -- see 11 module cards with relevance badges (when logged in with demo user)
4. Navigate to /ru/modules -- all text in Russian
5. Click category tabs -- cards filter correctly
6. Log out and visit /modules -- no crash, shows catalog without personalized scores
</verification>

<success_criteria>
- MOD-01: 11 modules visible across 4 categories (HR, Finance, Legal, Decision Making) with category filtering
- MOD-02: Each module card shows name, description, expected impact, and relevance to assessment results
- MOD-03: Modules sorted by relevance score -- struggling business areas surface higher-relevance modules first
- All text bilingual (EN/RU)
- No new npm dependencies
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-module-catalog/04-01-SUMMARY.md`
</output>
