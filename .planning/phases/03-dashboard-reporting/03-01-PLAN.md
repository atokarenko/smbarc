---
phase: 03-dashboard-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/role-filter.ts
  - src/components/dashboard/health-radar-chart.tsx
  - src/app/[locale]/(app)/dashboard/page.tsx
  - src/app/[locale]/(app)/dashboard/dashboard-content.tsx
  - src/messages/en.json
  - src/messages/ru.json
autonomous: true
requirements: [DASH-01, DASH-05]

must_haves:
  truths:
    - "User sees radar chart visualizing 5 dimension scores on dashboard overview"
    - "Dashboard sections reorder/emphasize based on user role (CEO/COO/CTO)"
    - "Dashboard loads user's own completed assessment first, falls back to demo company"
  artifacts:
    - path: "src/components/dashboard/role-filter.ts"
      provides: "Role configuration utility for section ordering and emphasis"
      exports: ["getRoleConfig", "RoleConfig"]
    - path: "src/components/dashboard/health-radar-chart.tsx"
      provides: "Radar chart for 5 business dimensions"
      exports: ["HealthRadarChart"]
    - path: "src/app/[locale]/(app)/dashboard/dashboard-content.tsx"
      provides: "Enhanced dashboard with charts and role-adapted layout"
      exports: ["DashboardContent"]
  key_links:
    - from: "src/app/[locale]/(app)/dashboard/page.tsx"
      to: "dashboard-content.tsx"
      via: "passes assessmentResults + role prop"
      pattern: "role=.*session.*role"
    - from: "src/components/dashboard/health-radar-chart.tsx"
      to: "recharts"
      via: "RadarChart from recharts wrapped in ChartContainer"
      pattern: "RadarChart"
---

<objective>
Install charting dependencies, create role-filter utility, and enhance the dashboard overview page with a radar chart for dimension scores and role-adapted section ordering.

Purpose: Transform the existing summary-card-only dashboard into a visual, role-aware overview that serves as the hub for all assessment data.
Output: Enhanced dashboard with radar chart, role-based layout, improved data loading (user assessment first, demo fallback).
</objective>

<execution_context>
@/Users/alextokarenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alextokarenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-reporting/03-RESEARCH.md

@src/app/[locale]/(app)/dashboard/page.tsx
@src/app/[locale]/(app)/dashboard/dashboard-content.tsx
@src/lib/demo-data.ts
@src/lib/assessment/types.ts
@src/messages/en.json
@src/messages/ru.json

<interfaces>
<!-- Key types from existing codebase -->

From src/lib/demo-data.ts:
```typescript
export interface MaturityScore {
  overall: number;
  dimensions: { operations: number; sales: number; finance: number; team: number; risks: number; };
}
export interface AssessmentResults {
  maturityScore: MaturityScore;
  automationRoadmap: RoadmapItem[];
  riskMap: RiskItem[];
  roiForecast: ROIForecast;
}
```

From src/lib/assessment/types.ts:
```typescript
export type { AssessmentResults, MaturityScore, RoadmapItem, RiskItem, ROIItem, ROIForecast } from "@/lib/demo-data";
```

Existing dashboard-content.tsx props:
```typescript
interface DashboardContentProps {
  assessmentResults: AssessmentResults | null;
  companyName: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create shared utilities</name>
  <files>
    package.json,
    src/components/dashboard/role-filter.ts,
    src/components/dashboard/health-radar-chart.tsx
  </files>
  <action>
1. Install recharts and @react-pdf/renderer (needed across phase):
   ```bash
   npm install recharts @react-pdf/renderer
   ```
   Use `--legacy-peer-deps` if peer dependency warnings block install.

2. Add shadcn chart, tabs, badge, and table components:
   ```bash
   npx shadcn@latest add chart tabs badge table --yes
   ```

3. Create `src/components/dashboard/role-filter.ts`:
   - Export type `Role = "ceo" | "coo" | "cto"`
   - Export interface `RoleConfig` with fields:
     - `dashboardSections: string[]` (ordered section IDs to display)
     - `roadmapSort: "impact" | "timeline" | "technical"`
     - `riskFocus: string[]` (risk categories to highlight first)
     - `roiView: "summary" | "detailed" | "technical"`
   - Export `ROLE_CONFIGS: Record<Role, RoleConfig>` with:
     - CEO: sections = ["healthScore", "roiSummary", "topRisks", "roadmapHighlights"], roadmapSort "impact", riskFocus ["financial", "reputational"], roiView "summary"
     - COO: sections = ["healthScore", "roadmapHighlights", "operationalRisks", "roiDetailed"], roadmapSort "timeline", riskFocus ["operational", "process"], roiView "detailed"
     - CTO: sections = ["healthScore", "technicalRoadmap", "dataRisks", "roiTechnical"], roadmapSort "technical", riskFocus ["data", "security", "integration"], roiView "technical"
   - Export `getRoleConfig(role: string): RoleConfig` that defaults to CEO config for unknown roles

4. Create `src/components/dashboard/health-radar-chart.tsx`:
   - "use client" component
   - Import RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar from recharts
   - Import ChartContainer, ChartTooltip, ChartTooltipContent from @/components/ui/chart
   - Props: `{ dimensions: Record<string, number> }` (the 5 dimension scores)
   - Transform dimensions object to array of `{ dimension: string, score: number }` for Recharts
   - Use ChartContainer with chartConfig defining "score" with color `hsl(var(--chart-1))`
   - RadarChart with PolarGrid, PolarAngleAxis (dataKey="dimension"), PolarRadiusAxis (domain [0,100], hide), Radar (dataKey="score", fill with opacity 0.5, stroke)
   - Height: 300px via className on ChartContainer
   - Use dimension display names from i18n if possible, or pass labels as prop
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>recharts and @react-pdf/renderer in package.json, shadcn chart/tabs/badge/table components exist in src/components/ui/, role-filter.ts exports getRoleConfig, health-radar-chart.tsx exports HealthRadarChart</done>
</task>

<task type="auto">
  <name>Task 2: Enhance dashboard overview page with charts and role awareness</name>
  <files>
    src/app/[locale]/(app)/dashboard/page.tsx,
    src/app/[locale]/(app)/dashboard/dashboard-content.tsx,
    src/messages/en.json,
    src/messages/ru.json
  </files>
  <action>
1. Update `dashboard/page.tsx` to:
   - Load user's OWN completed assessment first (query Assessment where userId=session.user.id AND status="COMPLETE", orderBy updatedAt desc), fall back to demo company. Use the pattern from research: `loadAssessmentData(userId)`.
   - Extract role from session: `(session?.user as { role?: string })?.role || "ceo"`
   - Pass `role` as new prop to DashboardContent

2. Update `dashboard-content.tsx` to:
   - Accept `role` prop (string, default "ceo")
   - Import HealthRadarChart from @/components/dashboard/health-radar-chart
   - Import getRoleConfig from @/components/dashboard/role-filter
   - After the overall score card, add the HealthRadarChart in a Card, showing the 5 dimension scores as a radar
   - Keep existing dimension score cards below the radar
   - Add a "quick links" row with 3 cards linking to /roadmap, /risks, /roi -- each showing count + brief label. Use Next.js Link component.
   - Order dashboard sections based on `getRoleConfig(role).dashboardSections`:
     - "healthScore" = score card + radar
     - "roiSummary" / "roiDetailed" / "roiTechnical" = ROI summary card (all show same data at overview level, just reordered)
     - "topRisks" / "operationalRisks" / "dataRisks" = risks summary card
     - "roadmapHighlights" / "technicalRoadmap" = roadmap summary card
   - The reordering is section-level: render sections in the order specified by role config

3. Add i18n keys to both en.json and ru.json under "dashboard" namespace:
   - `dashboard.radarTitle`: "Dimension Breakdown" / "Разбивка по направлениям"
   - `dashboard.viewRoadmap`: "View Roadmap" / "Посмотреть план"
   - `dashboard.viewRisks`: "View Risks" / "Посмотреть риски"
   - `dashboard.viewRoi`: "View ROI" / "Посмотреть ROI"
   - `dashboard.roleView`: "Viewing as {role}" / "Просмотр как {role}"
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx tsc --noEmit --pretty 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Dashboard page loads user assessment with demo fallback, passes role to content component. Radar chart renders 5 dimensions. Dashboard sections reorder based on role. i18n keys exist in both locales. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npm run build` succeeds
- Dashboard at /en/dashboard shows radar chart with demo data
- Role config utility returns different section orders for CEO/COO/CTO
</verification>

<success_criteria>
- Dashboard overview has a visible radar chart for 5 business dimensions
- Dashboard sections adapt ordering based on user role
- Data loading tries user's own assessment before falling back to demo
- All new text has EN and RU translations
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-reporting/03-01-SUMMARY.md`
</output>
