---
phase: 03-dashboard-reporting
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/[locale]/(app)/roadmap/page.tsx
  - src/app/[locale]/(app)/roadmap/roadmap-content.tsx
  - src/app/[locale]/(app)/risks/page.tsx
  - src/app/[locale]/(app)/risks/risk-map-content.tsx
  - src/app/[locale]/(app)/roi/page.tsx
  - src/app/[locale]/(app)/roi/roi-forecast-content.tsx
  - src/components/dashboard/roi-bar-chart.tsx
  - src/components/dashboard/priority-badge.tsx
  - src/messages/en.json
  - src/messages/ru.json
autonomous: true
requirements: [DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "User can view automation roadmap as a visual priority list with timeline indicators"
    - "User can view risk map with color-coded severity levels grouped by category"
    - "User can view ROI forecast with bar chart comparing current cost vs projected savings"
  artifacts:
    - path: "src/app/[locale]/(app)/roadmap/roadmap-content.tsx"
      provides: "Roadmap visualization with priority sorting and timeline badges"
      exports: ["RoadmapContent"]
    - path: "src/app/[locale]/(app)/risks/risk-map-content.tsx"
      provides: "Risk map with severity color coding and category grouping"
      exports: ["RiskMapContent"]
    - path: "src/app/[locale]/(app)/roi/roi-forecast-content.tsx"
      provides: "ROI forecast with bar chart and confidence indicators"
      exports: ["RoiForecastContent"]
    - path: "src/components/dashboard/roi-bar-chart.tsx"
      provides: "Horizontal bar chart for ROI comparison"
      exports: ["RoiBarChart"]
    - path: "src/components/dashboard/priority-badge.tsx"
      provides: "Reusable badge for priority/severity levels"
      exports: ["PriorityBadge"]
  key_links:
    - from: "src/app/[locale]/(app)/roadmap/page.tsx"
      to: "roadmap-content.tsx"
      via: "server loads assessment, passes automationRoadmap + role"
      pattern: "automationRoadmap.*role"
    - from: "src/app/[locale]/(app)/roi/roi-forecast-content.tsx"
      to: "src/components/dashboard/roi-bar-chart.tsx"
      via: "renders RoiBarChart with ROIItem[] data"
      pattern: "RoiBarChart.*items"
---

<objective>
Build the three assessment sub-pages (Roadmap, Risks, ROI) with full visualizations, replacing the current placeholders.

Purpose: Users need to drill into each assessment output area -- roadmap shows what to automate and when, risks show what to watch out for, ROI shows projected financial impact with charts.
Output: Three fully functional pages with charts, badges, role-aware sorting, and bilingual text.
</objective>

<execution_context>
@/Users/alextokarenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alextokarenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-reporting/03-RESEARCH.md
@.planning/phases/03-dashboard-reporting/03-01-SUMMARY.md

@src/lib/demo-data.ts
@src/lib/assessment/types.ts
@src/messages/en.json
@src/messages/ru.json

<interfaces>
<!-- Types from existing codebase that sub-pages consume -->

From src/lib/demo-data.ts:
```typescript
export interface RoadmapItem {
  name: string;
  description: string;
  priority: "high" | "medium" | "low";
  expectedImpact: string;
  timeline: string;
}

export interface RiskItem {
  category: string;
  level: "high" | "medium" | "low";
  description: string;
  mitigation: string;
}

export interface ROIItem {
  area: string;
  currentCost: number;
  projectedSaving: number;
  confidence: "high" | "medium" | "low";
}

export interface ROIForecast {
  totalSavings: number;
  timeframe: string;
  items: ROIItem[];
}
```

From src/components/dashboard/role-filter.ts (created in Plan 01):
```typescript
export type Role = "ceo" | "coo" | "cto";
export interface RoleConfig {
  dashboardSections: string[];
  roadmapSort: "impact" | "timeline" | "technical";
  riskFocus: string[];
  roiView: "summary" | "detailed" | "technical";
}
export function getRoleConfig(role: string): RoleConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Roadmap and Risk Map sub-pages</name>
  <files>
    src/app/[locale]/(app)/roadmap/page.tsx,
    src/app/[locale]/(app)/roadmap/roadmap-content.tsx,
    src/app/[locale]/(app)/risks/page.tsx,
    src/app/[locale]/(app)/risks/risk-map-content.tsx,
    src/components/dashboard/priority-badge.tsx,
    src/messages/en.json,
    src/messages/ru.json
  </files>
  <action>
1. Create `src/components/dashboard/priority-badge.tsx`:
   - "use client" component
   - Import Badge from @/components/ui/badge
   - Props: `{ level: "high" | "medium" | "low" }`
   - Map levels to Badge variants/colors:
     - high: destructive variant (red)
     - medium: secondary variant with amber text/bg styling via className
     - low: outline variant (neutral)
   - Display capitalized level text

2. Rewrite `roadmap/page.tsx` (replace PlaceholderPage):
   - Server component following established pattern (see dashboard/page.tsx)
   - Load assessment data: try user's completed assessment first, fall back to demo company (same loadAssessmentData pattern from Plan 01's dashboard/page.tsx)
   - Extract role from session
   - Import and render RoadmapContent with props: `items={assessment?.automationRoadmap ?? []}` and `role={role}`
   - Handle null assessment: RoadmapContent shows "complete assessment first" message

3. Create `roadmap/roadmap-content.tsx`:
   - "use client" component
   - Props: `{ items: RoadmapItem[]; role: string }`
   - Import getRoleConfig to determine sort order based on role:
     - "impact" (CEO): sort by priority (high first)
     - "timeline" (COO): sort by timeline string (shortest first -- parse "2-3 months" to numeric)
     - "technical" (CTO): sort by priority but group by technical complexity heuristic (just use priority for POC)
   - Render each item as a Card with:
     - PriorityBadge showing priority
     - Name as CardTitle
     - Description as CardDescription
     - Timeline in a small badge/tag (e.g., "2-3 months")
     - Expected impact in a highlighted text block
   - Empty state: message + link to /scan if no items
   - Use i18n for all static labels

4. Rewrite `risks/page.tsx` (same pattern as roadmap):
   - Server component, load assessment, extract role
   - Render RiskMapContent with `items={assessment?.riskMap ?? []}` and `role={role}`

5. Create `risks/risk-map-content.tsx`:
   - "use client" component
   - Props: `{ items: RiskItem[]; role: string }`
   - Use getRoleConfig to get `riskFocus` categories -- items matching focus categories sort to top
   - Group items by category, within each group sort by severity (high first)
   - Render each risk as a Card with:
     - PriorityBadge for severity level
     - Category as a small label/tag
     - Description as main text
     - Mitigation in a collapsible or distinct styled section (lighter bg, smaller text)
   - Color coding: high=red border-left or accent, medium=amber, low=neutral
   - Empty state: message + link to /scan

6. Add i18n keys to both en.json and ru.json:
   - `roadmap.title`: "Automation Roadmap" / "План автоматизации"
   - `roadmap.noData`: "Complete an assessment to see your automation roadmap." / "Пройдите оценку, чтобы увидеть план автоматизации."
   - `roadmap.priority`: "Priority" / "Приоритет"
   - `roadmap.timeline`: "Timeline" / "Сроки"
   - `roadmap.impact`: "Expected Impact" / "Ожидаемый эффект"
   - `roadmap.high`: "High" / "Высокий"
   - `roadmap.medium`: "Medium" / "Средний"
   - `roadmap.low`: "Low" / "Низкий"
   - `risks.title`: "Risk Map" / "Карта рисков"
   - `risks.noData`: "Complete an assessment to see your risk map." / "Пройдите оценку, чтобы увидеть карту рисков."
   - `risks.category`: "Category" / "Категория"
   - `risks.severity`: "Severity" / "Уровень"
   - `risks.mitigation`: "Mitigation" / "Меры снижения"
   - `risks.high`: "High" / "Высокий"
   - `risks.medium`: "Medium" / "Средний"
   - `risks.low`: "Low" / "Низкий"
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Roadmap page shows sorted automation items with priority badges and timelines. Risk map page shows severity-coded risks grouped by category with mitigations. Both pages load assessment data with demo fallback, adapt sorting by role. i18n keys in both locales.</done>
</task>

<task type="auto">
  <name>Task 2: Build ROI Forecast page with bar chart</name>
  <files>
    src/app/[locale]/(app)/roi/page.tsx,
    src/app/[locale]/(app)/roi/roi-forecast-content.tsx,
    src/components/dashboard/roi-bar-chart.tsx,
    src/messages/en.json,
    src/messages/ru.json
  </files>
  <action>
1. Create `src/components/dashboard/roi-bar-chart.tsx`:
   - "use client" component
   - Import BarChart, Bar, XAxis, YAxis, CartesianGrid from recharts
   - Import ChartContainer, ChartTooltip, ChartTooltipContent from @/components/ui/chart
   - Import ROIItem type from @/lib/assessment/types
   - Props: `{ items: ROIItem[] }`
   - chartConfig: currentCost with color chart-1, projectedSaving with color chart-2
   - Horizontal bar chart (layout="vertical"):
     - YAxis type="category" dataKey="area" width=120
     - XAxis type="number" with dollar formatter: `$${(v/1000).toFixed(0)}K`
     - Two bars: currentCost and projectedSaving, radius=4
     - ChartTooltip with ChartTooltipContent
   - Height: 350px via ChartContainer className

2. Rewrite `roi/page.tsx`:
   - Server component, load assessment data (same pattern), extract role
   - Render RoiForecastContent with `forecast={assessment?.roiForecast ?? null}` and `role={role}`

3. Create `roi/roi-forecast-content.tsx`:
   - "use client" component
   - Props: `{ forecast: ROIForecast | null; role: string }`
   - If no forecast: empty state with link to /scan
   - Summary section at top:
     - Total projected savings (large number, formatted as currency)
     - Timeframe label
     - Number of areas analyzed
   - RoiBarChart component showing all items
   - Below chart: a Card grid (or Table) listing each ROI item with:
     - Area name
     - Current cost formatted as currency
     - Projected saving formatted as currency
     - Confidence level as PriorityBadge (high=green, medium=amber, low=neutral -- different from risk where high=red)
     - Saving percentage: `(projectedSaving/currentCost * 100).toFixed(0)%`
   - Role-based emphasis (from getRoleConfig):
     - CEO (summary): focus on total savings, minimal detail
     - COO (detailed): show full table with all columns
     - CTO (technical): same as detailed but sort by highest confidence items first
   - The difference is subtle for POC -- all roles see the same data, just sorting varies

4. Add i18n keys to both en.json and ru.json:
   - `roi.title`: "ROI Forecast" / "Прогноз ROI"
   - `roi.noData`: "Complete an assessment to see your ROI forecast." / "Пройдите оценку, чтобы увидеть прогноз ROI."
   - `roi.totalSavings`: "Total Projected Savings" / "Общая прогнозируемая экономия"
   - `roi.timeframe`: "Timeframe" / "Период"
   - `roi.area`: "Area" / "Область"
   - `roi.currentCost`: "Current Cost" / "Текущие затраты"
   - `roi.projectedSaving`: "Projected Saving" / "Прогнозируемая экономия"
   - `roi.confidence`: "Confidence" / "Уверенность"
   - `roi.savingPercent`: "Saving %" / "% экономии"
   - `roi.areasAnalyzed`: "{count} areas analyzed" / "{count} областей проанализировано"
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx tsc --noEmit --pretty 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>ROI page shows total savings summary, horizontal bar chart comparing costs vs savings per area, and a detailed table with confidence badges. All text translated in EN/RU. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- /en/roadmap shows sorted automation items (no longer placeholder)
- /en/risks shows color-coded risk map (no longer placeholder)
- /en/roi shows bar chart and ROI details (no longer placeholder)
- All three pages handle null assessment data gracefully
</verification>

<success_criteria>
- Roadmap page displays automation items with priority badges and timeline indicators
- Risk map displays items with severity color coding, grouped by category
- ROI page displays bar chart and detailed table with confidence levels
- All three pages adapt sort/emphasis based on user role
- All new text has EN and RU translations
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-reporting/03-02-SUMMARY.md`
</output>
