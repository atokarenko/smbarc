---
phase: 01-platform-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/layout/sidebar.tsx
  - src/components/layout/header.tsx
  - src/components/layout/app-shell.tsx
  - src/components/theme/theme-toggle.tsx
  - src/app/[locale]/(app)/layout.tsx
  - src/app/[locale]/(app)/dashboard/page.tsx
  - src/app/[locale]/(app)/scan/page.tsx
  - src/app/[locale]/(app)/roadmap/page.tsx
  - src/app/[locale]/(app)/risks/page.tsx
  - src/app/[locale]/(app)/roi/page.tsx
  - src/app/[locale]/(app)/modules/page.tsx
  - src/app/[locale]/(app)/settings/page.tsx
  - src/app/[locale]/onboarding/page.tsx
  - src/messages/en.json
  - src/messages/ru.json
autonomous: false
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03

must_haves:
  truths:
    - "User sees a professional sidebar with all navigation items"
    - "Sidebar shows user's role badge and current demo company"
    - "Theme toggle switches between light and dark mode without flash"
    - "Language switcher in header toggles all UI text between EN and RU"
    - "All 7 sidebar pages are accessible and show placeholder content in correct language"
    - "Dashboard page shows demo company assessment summary when logged in as demo user"
  artifacts:
    - path: "src/components/layout/app-shell.tsx"
      provides: "Sidebar + content wrapper layout"
      contains: "Sidebar"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Main sidebar navigation with all menu items"
      contains: "navigation"
    - path: "src/components/layout/header.tsx"
      provides: "Top bar with locale switcher, theme toggle, user info"
      contains: "LocaleSwitcher"
    - path: "src/components/theme/theme-toggle.tsx"
      provides: "Light/dark theme toggle button"
      contains: "useTheme"
    - path: "src/app/[locale]/(app)/dashboard/page.tsx"
      provides: "Dashboard showing demo company data"
      contains: "assessmentResults"
  key_links:
    - from: "src/components/layout/sidebar.tsx"
      to: "src/i18n/navigation.ts"
      via: "Link component for locale-aware navigation"
      pattern: "import.*Link.*navigation"
    - from: "src/components/layout/header.tsx"
      to: "src/components/i18n/locale-switcher.tsx"
      via: "includes locale switcher"
      pattern: "LocaleSwitcher"
    - from: "src/components/layout/header.tsx"
      to: "src/components/theme/theme-toggle.tsx"
      via: "includes theme toggle"
      pattern: "ThemeToggle"
    - from: "src/app/[locale]/(app)/layout.tsx"
      to: "src/lib/auth.ts"
      via: "session check for protected routes"
      pattern: "auth\\.api\\.getSession"
    - from: "src/app/[locale]/(app)/dashboard/page.tsx"
      to: "src/lib/db/prisma.ts"
      via: "loads demo company data"
      pattern: "prisma\\.company"
---

<objective>
Build the complete app shell with sidebar navigation, header (locale/theme controls), placeholder pages, and a dashboard that displays demo company assessment data.

Purpose: This is the visual and navigational foundation -- after this plan, the app looks and feels like a professional "consulting-grade" tool. Co-founders can navigate the full sidebar, switch languages, toggle themes, and see demo assessment results on the dashboard.
Output: Fully navigable app shell with sidebar, 7 routed pages, theme/language controls, and dashboard rendering demo data.
</objective>

<execution_context>
@/Users/alextokarenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alextokarenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-01-SUMMARY.md
@.planning/phases/01-platform-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build app shell with sidebar, header, theme toggle, and all pages</name>
  <files>
    src/components/layout/sidebar.tsx, src/components/layout/header.tsx,
    src/components/layout/app-shell.tsx, src/components/theme/theme-toggle.tsx,
    src/app/[locale]/(app)/layout.tsx,
    src/app/[locale]/(app)/dashboard/page.tsx,
    src/app/[locale]/(app)/scan/page.tsx,
    src/app/[locale]/(app)/roadmap/page.tsx,
    src/app/[locale]/(app)/risks/page.tsx,
    src/app/[locale]/(app)/roi/page.tsx,
    src/app/[locale]/(app)/modules/page.tsx,
    src/app/[locale]/(app)/settings/page.tsx,
    src/app/[locale]/onboarding/page.tsx,
    src/messages/en.json, src/messages/ru.json
  </files>
  <action>
    1. Create theme toggle at `src/components/theme/theme-toggle.tsx`:
       - Client component using `useTheme` from next-themes
       - Sun/Moon icons from lucide-react
       - Ghost button that toggles between light and dark
       - Follow exact pattern from RESEARCH.md

    2. Create sidebar at `src/components/layout/sidebar.tsx`:
       - Use shadcn Sidebar component (SidebarProvider, Sidebar, SidebarContent, SidebarMenu, SidebarMenuItem, SidebarMenuButton)
       - Navigation items: Dashboard (LayoutDashboard icon), Assessment (ClipboardCheck), Roadmap (Map), Risks (ShieldAlert), ROI (TrendingUp), Modules (Boxes), Settings (Settings)
       - Use `Link` from `@/i18n/navigation` for locale-aware links
       - Use `useTranslations('common.navigation')` for item labels
       - Active state highlighting based on current pathname
       - Bottom section: user info with RoleBadge, company name
       - Professional appearance -- clean lines, subtle hover states
       - Style: Linear/Notion-inspired (per user decision)

    3. Create header at `src/components/layout/header.tsx`:
       - Top bar with: page title (left), LocaleSwitcher + ThemeToggle + user avatar (right)
       - Use shadcn Avatar with dropdown menu for user actions (settings, logout)
       - Include the sidebar mobile trigger (SidebarTrigger) for responsive design

    4. Create app shell at `src/components/layout/app-shell.tsx`:
       - Wraps Sidebar + Header + main content area
       - Uses SidebarProvider from shadcn
       - Responsive: sidebar collapses to icons on smaller screens, sheet on mobile

    5. Create protected layout at `src/app/[locale]/(app)/layout.tsx`:
       - Server component that checks session via `auth.api.getSession({ headers: await headers() })`
       - If no session: redirect to /[locale]/login
       - If session exists: render AppShell with children
       - Pass user data (role, name) to AppShell via context or props

    6. Create dashboard page at `src/app/[locale]/(app)/dashboard/page.tsx`:
       - Server component that loads the user's associated demo company
       - If demo user: find the demo company and parse assessmentResults
       - Display summary cards using shadcn Card:
         - AI Maturity Score (overall number + level name)
         - Key metrics (strategy, adoption, risk management, ROI tracking, governance)
         - Quick summary of roadmap items count, risk count, projected ROI
       - All text translated via useTranslations
       - Add dashboard-specific translations to en.json and ru.json

    7. Create placeholder pages for: scan, roadmap, risks, roi, modules, settings
       - Each is a server component with setRequestLocale
       - Shows page title (translated) and a "Coming soon" message (translated)
       - Settings page additionally shows: current role, role change dropdown (Select), language selection
       - Use consistent layout with Card wrapper

    8. Create onboarding page at `src/app/[locale]/onboarding/page.tsx`:
       - Welcome wizard: company name input, role select (CEO/COO/CTO), industry select
       - Use shadcn Select, Input, Button
       - On submit: save to user profile and redirect to dashboard
       - For demo mode: pre-fill with demo company data, allow quick "Continue"
       - All labels translated

    9. Update `src/messages/en.json` and `src/messages/ru.json` with:
       - dashboard: title, maturityScore, dimensions (strategy, adoption, riskManagement, roiTracking, governance), roadmapItems, risks, projectedRoi, level names (beginner, developing, intermediate, advanced, leader)
       - pages: scan, roadmap, risks, roi, modules section titles + "coming soon"
       - settings: title, role, language, profile
       - onboarding: all wizard labels

    IMPORTANT per user decision: Sidebar should feel professional -- "consulting-grade" tool appearance. Use subtle colors, clean typography, proper spacing. Accent color: choose a professional blue or teal (Claude's discretion).
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - App shell renders with sidebar, header, and content area
    - All 7 navigation items in sidebar link to correct pages
    - Sidebar shows role badge and user info
    - Theme toggle switches light/dark without flash
    - Locale switcher toggles all UI text between EN and RU
    - Dashboard renders demo company assessment summary with maturity score cards
    - Placeholder pages show translated "Coming soon" content
    - Onboarding wizard renders with company/role/industry fields
    - Settings page shows role changer and language selection
    - Build succeeds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 application</name>
  <action>
    Human verifies the complete Phase 1 application visually and functionally.

    What was built: Complete Phase 1 application -- Next.js app with sidebar navigation (Linear/Notion style), auth via demo persona selector, i18n (EN/RU), theme toggle (light/dark), AI provider abstraction with health check, dashboard showing demo company assessment results, and all navigation pages.

    How to verify:
    1. Start the app: `npm run dev` in the project root
    2. Visit http://localhost:3000 -- should redirect to /en
    3. On landing page: click "Try Demo" to go to login/demo selector
    4. Select a demo role (CEO, COO, or CTO) -- should create session and redirect to dashboard
    5. On dashboard: verify maturity score and summary cards display demo data
    6. Click through ALL sidebar items: Dashboard, Assessment, Roadmap, Risks, ROI, Modules, Settings
    7. In header: click locale switcher -- all text should switch to Russian (or English)
    8. In header: click theme toggle -- app should switch between light and dark mode
    9. In Settings: verify role is shown and can be changed
    10. Check sidebar feels professional and "consulting-grade"
    11. Resize browser to mobile width -- sidebar should collapse
    12. (Optional) Visit http://localhost:3000/api/ai/health to check AI connectivity
  </action>
  <verify>Manual visual and functional verification by user</verify>
  <done>User confirms app looks professional, all navigation works, i18n toggles correctly, theme switches without flash, demo data displays on dashboard</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Full navigation flow: landing -> demo select -> dashboard -> all pages
3. i18n: all visible text switches between EN and RU
4. Theme: light/dark toggle works without flash
5. Dashboard: shows maturity score and summary for demo company
6. Sidebar: professional appearance with correct icons and active states
7. Mobile: sidebar collapses appropriately
</verification>

<success_criteria>
- Professional app shell with sidebar navigation (Linear/Notion style)
- All 7 sidebar pages accessible with correct translations
- Theme toggle (light/dark) functional
- Language switcher (EN/RU) toggles all UI text
- Dashboard displays demo company assessment data
- Onboarding wizard functional
- Settings page shows role and language options
- Human verification confirms professional appearance and full functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-03-SUMMARY.md`
</output>
