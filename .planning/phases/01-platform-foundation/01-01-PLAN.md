---
phase: 01-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - vitest.config.ts
  - .env
  - .env.example
  - prisma/schema.prisma
  - src/lib/db/prisma.ts
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/i18n/navigation.ts
  - src/messages/en.json
  - src/messages/ru.json
  - src/middleware.ts
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/components/i18n/locale-switcher.tsx
  - src/__tests__/i18n.test.ts
  - src/__tests__/demo-data.test.ts
  - src/__tests__/auth.test.ts
  - src/__tests__/ai-provider.test.ts
  - tests/setup.ts
autonomous: true
requirements:
  - PLAT-01

must_haves:
  truths:
    - "Next.js dev server starts without errors on localhost:3000"
    - "Visiting /en and /ru renders page in correct language"
    - "Locale switcher toggles between EN and RU"
    - "Prisma schema is valid and database is created"
    - "Vitest runs and discovers test stubs"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "Locale routing config (en, ru)"
      contains: "defineRouting"
    - path: "src/i18n/request.ts"
      provides: "Request locale resolution"
      contains: "getRequestConfig"
    - path: "src/i18n/navigation.ts"
      provides: "Locale-aware navigation exports"
      contains: "createNavigation"
    - path: "src/messages/en.json"
      provides: "English translations"
      contains: "appName"
    - path: "src/messages/ru.json"
      provides: "Russian translations"
      contains: "appName"
    - path: "src/middleware.ts"
      provides: "next-intl middleware for locale routing"
      contains: "createMiddleware"
    - path: "prisma/schema.prisma"
      provides: "Database schema with User, Session, Account, Verification, Company models"
      contains: "model Company"
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
  key_links:
    - from: "src/middleware.ts"
      to: "src/i18n/routing.ts"
      via: "import routing config"
      pattern: "import.*routing"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/i18n/request.ts"
      via: "NextIntlClientProvider with messages"
      pattern: "NextIntlClientProvider"
    - from: "src/components/i18n/locale-switcher.tsx"
      to: "src/i18n/navigation.ts"
      via: "useRouter and usePathname from navigation"
      pattern: "useRouter.*usePathname"
---

<objective>
Bootstrap the Next.js project with all dependencies, Prisma database schema, next-intl i18n (EN/RU), and Vitest test infrastructure.

Purpose: Create the foundational scaffold that all subsequent plans build on -- no features can be implemented without a running project, database, translations, and test framework.
Output: Running Next.js dev server with locale routing, SQLite database with schema, and test stubs for all PLAT requirements.
</objective>

<execution_context>
@/Users/alextokarenko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alextokarenko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project, install dependencies, set up Prisma and Vitest</name>
  <files>
    package.json, tsconfig.json, next.config.ts, vitest.config.ts, .env, .env.example,
    prisma/schema.prisma, src/lib/db/prisma.ts, src/lib/utils.ts,
    tests/setup.ts, src/__tests__/i18n.test.ts, src/__tests__/auth.test.ts,
    src/__tests__/demo-data.test.ts, src/__tests__/ai-provider.test.ts
  </files>
  <action>
    1. Create Next.js 15 project in the repo root (NOT in a subdirectory -- files go directly in the repo root):
       `npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --no-git`
       Use `--no-git` since git is already initialized. If create-next-app refuses to run in non-empty dir, create in a temp dir and move files.

    2. Install all dependencies per RESEARCH.md:
       ```
       npm install better-auth next-intl ai @ai-sdk/openai zod prisma @prisma/client better-sqlite3 next-themes lucide-react
       npm install -D @types/better-sqlite3 vitest @testing-library/react @testing-library/jest-dom jsdom
       ```

    3. Initialize shadcn/ui:
       `npx shadcn@latest init -y`
       Then add components: `npx shadcn@latest add button card avatar dropdown-menu select separator sheet sidebar navigation-menu badge`

    4. Create Prisma schema at `prisma/schema.prisma` with ALL models from RESEARCH.md:
       - User (id, name, email, emailVerified, image, role default "ceo", timestamps)
       - Session (id, userId, token unique, expiresAt, ipAddress, userAgent, timestamps)
       - Account (id, userId, accountId, providerId, accessToken, refreshToken, accessTokenExpiresAt, refreshTokenExpiresAt, scope, idToken, password, timestamps)
       - Verification (id, identifier, value, expiresAt, timestamps)
       - Company (id, name, industry, size, description, isDemo default false, locale default "en", assessmentResults as String?, timestamps)
       Use SQLite provider with DATABASE_URL from env.

    5. Create `.env` with:
       ```
       DATABASE_URL="file:./dev.db"
       AI_BASE_URL="http://localhost:3456/v1"
       AI_API_KEY="not-needed-for-local-proxy"
       AI_MODEL="claude-sonnet-4-20250514"
       ```
       Create `.env.example` with same keys but placeholder values. Add `.env` to `.gitignore`.

    6. Create Prisma client singleton at `src/lib/db/prisma.ts` using the standard globalThis pattern to avoid hot-reload connection issues.

    7. Run `npx prisma db push` to create the SQLite database.

    8. Create `vitest.config.ts` with:
       - jsdom environment
       - Path alias `@/` pointing to `src/`
       - Setup file: `tests/setup.ts`

    9. Create `tests/setup.ts` importing `@testing-library/jest-dom`.

    10. Create test stub files (each with a single `describe` block and `test.todo` placeholders):
        - `src/__tests__/i18n.test.ts` -- "locale switching", "translations load for en", "translations load for ru"
        - `src/__tests__/auth.test.ts` -- "demo persona selector creates session", "role is stored in session"
        - `src/__tests__/demo-data.test.ts` -- "seed data creates 3 demo companies", "demo companies have assessment results"
        - `src/__tests__/ai-provider.test.ts` -- "provider connects with baseURL", "health check returns status"

    11. Verify `npx vitest run` executes without error (todos will be pending, not failing).

    IMPORTANT: Use `next-intl` plugin in `next.config.ts`:
    ```typescript
    import createNextIntlPlugin from 'next-intl/plugin';
    const withNextIntl = createNextIntlPlugin();
    const nextConfig = {};
    export default withNextIntl(nextConfig);
    ```
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npx prisma db push --accept-data-loss 2>&1 | tail -3 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `npm run dev` starts Next.js on localhost:3000 without errors
    - Prisma schema valid, dev.db created with all 5 tables
    - `npx vitest run` discovers 4 test files with pending todos (0 failures)
    - All dependencies installed in package.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up next-intl i18n with EN/RU translations and locale switcher</name>
  <files>
    src/i18n/routing.ts, src/i18n/request.ts, src/i18n/navigation.ts,
    src/messages/en.json, src/messages/ru.json,
    src/middleware.ts,
    src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx,
    src/components/i18n/locale-switcher.tsx
  </files>
  <action>
    1. Create `src/i18n/routing.ts` using `defineRouting` with locales ['en', 'ru'], defaultLocale 'en'.

    2. Create `src/i18n/request.ts` using `getRequestConfig` that resolves locale from request and loads messages from `src/messages/{locale}.json`. Follow the exact pattern from RESEARCH.md.

    3. Create `src/i18n/navigation.ts` exporting `Link`, `redirect`, `usePathname`, `useRouter`, `getPathname` from `createNavigation(routing)`.

    4. Create `src/middleware.ts` using `createMiddleware(routing)` with matcher `'/((?!api|_next|_vercel|.*\\..*)*)'`.

    5. Create `src/messages/en.json` with the full translation structure from RESEARCH.md:
       - common: appName, navigation (dashboard, scan, roadmap, risks, roi, modules, settings), roles (ceo, coo, cto), actions (login, register, logout, tryDemo, switchLanguage)
       - demo: title, subtitle, selectRole, viewResults
       - onboarding: welcome, companyName, selectRole, selectIndustry, continue
       - pages: placeholder text for "coming soon" pages

    6. Create `src/messages/ru.json` with Russian translations matching the same structure (use translations from RESEARCH.md).

    7. Create root locale layout at `src/app/[locale]/layout.tsx`:
       - Import `NextIntlClientProvider` and pass messages
       - Import `getMessages`, `setRequestLocale` from next-intl/server
       - Generate static params for ['en', 'ru']
       - Call `setRequestLocale(locale)` before rendering
       - Wrap children in NextIntlClientProvider
       - Include html lang attribute from locale
       - Include ThemeProvider from next-themes (attribute="class", defaultTheme="system", enableSystem)

    8. Create landing page at `src/app/[locale]/page.tsx`:
       - Simple page showing app name and "Try Demo" / "Log in" buttons using translations
       - Include LocaleSwitcher component
       - Use `setRequestLocale(locale)` for static rendering

    9. Create `src/components/i18n/locale-switcher.tsx` as a client component:
       - Uses `useLocale` from next-intl
       - Uses `useRouter`, `usePathname` from `@/i18n/navigation`
       - Toggles between 'en' and 'ru' via `router.replace(pathname, { locale: nextLocale })`
       - Renders a Button showing "RU" when current is en, "EN" when current is ru

    10. Remove the default `src/app/layout.tsx` and `src/app/page.tsx` created by create-next-app (replaced by `[locale]` versions).

    11. Verify: `npm run dev` should serve pages at /en and /ru with correct translations.
  </action>
  <verify>
    <automated>cd /Users/alextokarenko/Projects/RaccoonSoft/B2B_Automate_Business && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - /en renders English text, /ru renders Russian text
    - Locale switcher component exists and toggles locale
    - Middleware redirects / to /en (default locale)
    - `npm run build` succeeds without errors
    - Layout includes NextIntlClientProvider and ThemeProvider
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Visiting http://localhost:3000 redirects to /en
3. /en shows English content, /ru shows Russian content
4. `npx prisma db push` succeeds (schema valid)
5. `npx vitest run` runs with 0 failures
6. `npm run build` succeeds
</verification>

<success_criteria>
- Running Next.js 15 application with all Phase 1 dependencies installed
- SQLite database with User, Session, Account, Verification, Company tables
- EN/RU locale routing working with translations
- Locale switcher component functional
- Vitest configured with test stubs for all PLAT requirements
- ThemeProvider wired in root layout
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-01-SUMMARY.md`
</output>
